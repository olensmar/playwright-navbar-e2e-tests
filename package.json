{
  "name": "playwright-navbar-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright end-to-end tests validating main navbar navigation",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
